# Este servicio ahora reutiliza exclusivamente el archivo .env que vive en la raíz del monorepo.
# Si necesitas variables distintas para entornos específicos, crea un archivo .env en la raíz
# (o usa variables de entorno al ejecutar el servicio).