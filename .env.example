# ====== Postgres superuser (para init/seed) ======
PGSUPER=postgres
PGSUPER_PASS=postgres123
PGHOST=127.0.0.1
PGPORT=5432

# ====== App DB/Role ======
DBNAME=heartguard
DBUSER=heartguard_app
DBPASS=dev_change_me

# Cadena para apps (sin interpolación)
DATABASE_URL=postgres://heartguard_app:dev_change_me@127.0.0.1:5432/heartguard?sslmode=disable

# ====== Backend ======
ENV=dev
HTTP_ADDR=:8080
SUPERADMIN_TEST_TOKEN=let-me-in
ACCESS_TOKEN_SECRET=dev-secret-change
